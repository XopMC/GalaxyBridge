<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <application>
        <!-- Variant-specific: neither this entry point nor its bytecode is in any release. -->
        <receiver
            android:name=".transport.DownloadsProviderQaReceiver"
            android:exported="true"
            android:permission="android.permission.DUMP">
            <intent-filter>
                <action android:name="com.xopmc.galaxybridge.QA_DOWNLOADS_PROVIDER" />
            </intent-filter>
        </receiver>
        <activity
            android:name=".service.TextInputQaActivity"
            android:excludeFromRecents="true"
            android:exported="true"
            android:permission="android.permission.DUMP"
            android:theme="@android:style/Theme.Material.Light.NoActionBar" />
        <activity
            android:name=".service.AudioVideoQaActivity"
            android:excludeFromRecents="true"
            android:exported="true"
            android:permission="android.permission.DUMP"
            android:theme="@android:style/Theme.Material.Light.NoActionBar" />
    </application>
</manifest>
